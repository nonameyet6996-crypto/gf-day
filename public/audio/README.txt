Drop your song here as:
  our-song.mp3

(Or rename the path in components/MusicPlayer.tsx — look for
AUDIO_SRC — to match whatever filename you're using.)

Also update SONG_TITLE and SONG_ARTIST at the top of that file so the
island in the top-right shows the right name.
